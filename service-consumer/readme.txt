配置文件参数：				
	eureka.client.service-url.defaultZone		指定服务注册中心的地址
	
	turbine.app-config：指定需要收集其监控信息的服务名
	turbine.cluster-name-expression：指定聚合集群名称
	turbine.combine-host-port：设置为true，让同一主机上的服务通过主机名与端口号的组合来进行区分，默认情况下会以host来区分不同的服务。
	



